# Copy to .env for local runs. On the VPS use /etc/hng-stage1-api.env (see README).
HOST=127.0.0.1
PORT=3000

# Required for GET /me — use your real details for submission
ME_NAME=Your Full Name
ME_EMAIL=you@example.com
ME_GITHUB=https://github.com/yourusername
